[project]
name = "dotfiles"
version = "0.1.0"
description = "Simple configuration and utilities for Arch on Niri"
readme = "README.md"
requires-python = ">=3.14"

[tool.uv.workspace]
members = ["python_utils/*"]

